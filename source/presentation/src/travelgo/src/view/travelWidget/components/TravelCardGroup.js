//@flow
import React from 'react';
import styled from 'styled-components';
import { Card, CardHeader, CardDescription, CardContent, CardMeta, FeedDate} from '../../../gui';
import TraveCard from './TravelCard';

type Props = {|

|};



const TravelCardGroup = ({ destination, departureDate, imageSrc, note, returnDate }: Props) => {

    return (
        <Card>
           
        </Card>
    )
};

export default TravelCardGroup;